{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "start": "npm run build && npm start --prefix packages/home",
    "deploy": "npm run build &&gh-pages -d packages/home/build"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "lerna": "^4.0.0"
  }
}
